# 재사용 프롬프트 모음

## 1. 프로젝트 재시작 프롬프트
```markdown
이 프로젝트 폴더의 `_handoff/AGENTS.md`, `_handoff/folder_map.md`, `_handoff/handoff.md`를 먼저 읽고,
`codex-current/files-mentioned-by-the-user-pptx/outputs/pack-ai-agent-stage1`를 기준으로
현재 화면 구조와 수정 포인트를 10줄 이내로 요약해줘.
```

## 2. 코드 수정 요청 프롬프트
```markdown
`pack-ai-agent-stage1` 폴더의 HTML/CSS/JS만 최소 수정해서 반영해줘.
모바일 우선으로 보고, 변경한 파일과 직접 확인한 결과만 짧게 알려줘.
```

## 3. 오류 해결 요청 프롬프트
```markdown
현재 깨지는 화면을 먼저 재현하고,
원인 후보를 2개 이내로 좁힌 다음,
가장 영향이 작은 수정으로 고쳐줘.
고친 뒤 어떤 화면에서 확인했는지 함께 알려줘.
```

## 4. 배포 전 점검 프롬프트
```markdown
`pack-ai-agent-stage1` 기준으로 배포 전 점검을 해줘.
1) 민감정보 제외 대상
2) 깨질 가능성이 있는 상대경로
3) 모바일 화면 겹침 여부
4) 1단계 -> 2단계 -> 3단계 이동 흐름
순으로 확인해줘.
```

## 5. 문서 정리 프롬프트
```markdown
지금 수정된 내용을 `_handoff/handoff.md`와 `_handoff/decisions.md`에 반영할 항목만 추려서
짧은 bullet로 정리해줘. 실제 파일 수정은 하지 말고 초안만 보여줘.
```

## 6. 테스트 실행 프롬프트
```markdown
`test/testcase.md`와 `test/smoke-result.md`를 먼저 읽고,
현재 수정이 스모크 테스트 재실행이 필요한 수준인지 판단해줘.
필요하면 최소 범위 회귀 테스트만 실행해줘.
```

## 7. 개인 계정 이관 점검 프롬프트
```markdown
이 프로젝트를 개인 GitHub + 개인 Vercel로 옮긴다고 가정하고,
복사해야 할 폴더 / 제외해야 할 폴더 / 새로 발급할 환경변수 / 첫 실행 순서를
체크리스트 형태로 정리해줘.
```

## 8. 다음 개선 요청 프롬프트
```markdown
현재 프로토타입에서 가입완료율을 높일 만한 다음 개선안을 3개만 제안해줘.
형식은 여정 단계 -> 병목 -> 원인 가설 -> 검증 방법 순서로 써줘.
```

## 안전 원칙
- 실제 API key, 계정 정보, 회사 내부 데이터가 들어간 프롬프트는 재사용하지 않는다.
- 민감한 문자열은 변수명 또는 익명 텍스트로 바꾼다.
